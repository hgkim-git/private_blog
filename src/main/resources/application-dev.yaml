spring:
  datasource:
    #    url: ${TEST_DB_URL}
    #    username: ${TEST_DB_USER}
    #    password: ${TEST_DB_PASSWORD}
    # Syntax error가 보이면 build.gradle에 dependency 추가
    #    driver-class-name: org.h2.Driver
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      # validate: 엔티티와 테이블이 정상 매핑되었는지만 확인 (운영 환경 권장)
      # update: 엔티티 변경사항을 자동 반영 (개발 환경에서만 사용)
      # create: 시작 시 테이블 삭제 후 재생성 (위험, 테스트용)
      # none: 아무것도 하지 않음
      ddl-auto: update
  thymeleaf:
    cache: false
    prefix: file:src/main/resources/templates/
  web:
    resources:
      static-locations: file:src/main/resources/static/

jwt:
  secret-key: OE9256/E7NmKlhCYnakPTCGb9/oYww5BJoB1xaamT16LR7QAroL+pE5UUba9nnQ5 # prod 에선 환경 변수 처리 필요
  access-token-expiration: 86400000 # access token 만료 시간(ms) 관리자 페이지라 혼자만 로그인하니까 하루로 잡음
  cookie-name: access-token
  use-secure-cookie: false # http: false / https: true

logging:
  level:
    root: INFO
    io.github.hgkimer.privateblog: DEBUG
    p6spy: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} %clr(%-5level) %clr([%15.15thread]){cyan} %clr(%-40.40logger{39}){blue} - %msg%n"